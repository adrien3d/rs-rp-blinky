sudo apt update
cd /home
mkdir dev
cd dev
mkdir rpi && cd rpi
mkdir pico && cd pico
ls
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
cargo
cd /home/dev/sonelio/rpi/pico
git remote -v
..
rm pico
rm -R pico
cargo new
cargo new pico
cd pico
cd ..
rm -R pico
cargo new pico
cd pico 
probe-rs
rustup update
curl --proto '=https' --tlsv1.2 -LsSf https://github.com/probe-rs/probe-rs/releases/latest/download/probe-rs-tools-installer.sh | sh
probe-rs
cargo run
sudo apt update\nsudo apt install build-essential\n
cargo run
sudo apt install gcc\n
cargo install flip-link
ls
clear
cargo run
lsusb
ls
probe-rs
probe-rs list
df
service udev status\n
nano /etc/wsl.conf
lsusb
apt install lsusb
probe-rs list
git add .
ls
gst
git add .
git commit -am "feat: added code from rp2040 template"
git push
git push --set-upstream origin pico-one
cabu
cargo build
cacl
cargo clean
cargo update
cargo clean
clear && cargo build
sudo apt install pkg-config -y
sudo apt install libudev-dev -y
cargo install elf2uf2-rs
# or: cargo install uf2conv


cargo install elf2uf2-rs